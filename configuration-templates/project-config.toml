# INACTIVE TEMPLATE — verify your installed client and merge project-scoped keys only.
# Do not overwrite ~/.codex/config.toml, secrets, sandbox or approval settings.
# Select Sol Medium separately for the initial Phase 00 architecture review if appropriate.
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"

[agents]
enabled = true
max_concurrent_threads_per_session = 2
default_subagent_model = "gpt-5.6-luna"
default_subagent_reasoning_effort = "medium"
