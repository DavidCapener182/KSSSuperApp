# TASK-UI11 owned paths

Isolated worktree: `/private/tmp/kss-task-ui11-shell-nav` (`task-ui11-shell-nav`). Starting snapshot: `be3fb185249ecb10259d3bc944a1258131834341`.

Owned implementation: `src/components/enterprise-shell.tsx`, shell-specific rules in `src/app/globals.css`.

Owned documentation and evidence: this manifest, `docs/ui/TASK-UI11-SHELL-PROTOTYPE-REPORT.md`, `docs/delivery/TASK-UI11-REPORT.md`, and five screenshots plus `capture-checks.json` and `rollout-checks.json` under `docs/ui/evidence/ui11/`.

No capability map, server route, data contract, Supabase schema, other lane's module styling, or deployment is in scope.
