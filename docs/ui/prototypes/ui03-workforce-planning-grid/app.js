/* Standalone synthetic interaction artifact. No network calls or product writes. */
const BASE = "2026-10-12";
const days = ["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"];
const assignments = (...entries) => entries.map(([name, status, note = ""]) => ({ name, status, note }));
const duties = [
  { id:"E-101", source:"EVENT", client:"Northshore Events", site:"Riverside Arena", context:"Saturday Stadium Cover", role:"Entry Steward", area:"North entrance", date:"2026-10-17", time:"09:00–18:00", report:"08:30", required:3, people:assignments(["Samira Cole","ACCEPTED"],["Lewis Price","ALLOCATED","Event/static clash"]), flags:["clash"], sourceId:"EV-104 / REQ-101" },
  { id:"E-102", source:"EVENT", client:"Northshore Events", site:"Riverside Arena", context:"Saturday Stadium Cover", role:"Stand Security", area:"East stand", date:"2026-10-17", time:"10:00–19:00", report:"09:30", required:2, people:assignments(["Jane Ellis","ACCEPTED"],["Owen Blake","ALLOCATED"]), flags:[], sourceId:"EV-104 / REQ-102" },
  { id:"E-103", source:"EVENT", client:"Northshore Events", site:"Dockside Grounds", context:"Lantern Festival", role:"Gate Steward", area:"Main gate", date:"2026-10-17", time:"12:00–22:00", report:"11:30", required:2, people:assignments(["Priya Shah","ACCEPTED","Approved Time Away conflict"]), flags:["leave"], sourceId:"EV-118 / REQ-103" },
  { id:"E-104", source:"EVENT", client:"Northshore Events", site:"Dockside Grounds", context:"Lantern Festival", role:"Patrol Officer", area:"South perimeter", date:"2026-10-17", time:"14:00–23:00", report:"13:30", required:2, people:assignments(["Theo Marsh","ACCEPTED"],["Mia Clarke","ALLOCATED"]), flags:[], sourceId:"EV-118 / REQ-104" },
  { id:"S-201", source:"SITE_SHIFT", client:"Civic Facilities", site:"West Gate", context:"West Gate Service", role:"Security Officer", area:"Front desk", date:"2026-10-17", time:"12:00–20:00", report:"11:45", required:2, people:assignments(["Lewis Price","ACCEPTED","Event/static clash"]), flags:["clash"], sourceId:"SS-021 / DEM-201" },
  { id:"S-202", source:"SITE_SHIFT", client:"Civic Facilities", site:"West Gate", context:"West Gate Service", role:"Supervisor", area:"Building lead", date:"2026-10-17", time:"08:00–18:00", report:"07:45", required:1, people:assignments(["Carla Reid","ACCEPTED"]), flags:[], sourceId:"SS-021 / DEM-202" },
  { id:"E-105", source:"EVENT", client:"Civic Facilities", site:"Civic Hall", context:"Community Expo", role:"Reception Steward", area:"West doors", date:"2026-10-17", time:"09:00–17:00", report:"08:45", required:2, people:assignments(["Nia Ahmed","ALLOCATED","Unavailable declaration"]), flags:["availability"], sourceId:"EV-131 / REQ-105" },
  { id:"E-106", source:"EVENT", client:"Civic Facilities", site:"Civic Hall", context:"Community Expo", role:"Patrol Officer", area:"Hall perimeter", date:"2026-10-17", time:"10:00–18:00", report:"09:30", required:1, people:assignments(), flags:[], history:["Declined response retained in source history"], sourceId:"EV-131 / REQ-106" },
  { id:"E-107", source:"EVENT", client:"Harbour Services", site:"Harbour Park", context:"River Run", role:"Perimeter Steward", area:"Course east", date:"2026-10-17", time:"07:00–15:00", report:"06:30", required:3, people:assignments(["Omar Lewis","ACCEPTED"],["Alice Ford","ALLOCATED"]), flags:[], sourceId:"EV-145 / REQ-107" },
  { id:"S-203", source:"SITE_SHIFT", client:"Harbour Services", site:"Harbour Park", context:"Harbour Park Service", role:"Night Watch", area:"Park office", date:"2026-10-17", time:"20:00–Sun 06:00", report:"19:45", required:2, people:assignments(["Ben Doyle","ACCEPTED"]), flags:[], sourceId:"SS-034 / DEM-203" },
  { id:"S-204", source:"SITE_SHIFT", client:"Civic Facilities", site:"West Gate", context:"West Gate Service", role:"Night Watch", area:"Gatehouse", date:"2026-10-16", time:"Fri 18:00–Sat 06:00", report:"Fri 17:45", required:1, people:assignments(["Erin Walsh","ACCEPTED"]), flags:[], overnight:true, sourceId:"SS-021 / DEM-204" },
  { id:"E-108", source:"EVENT", client:"Northshore Events", site:"Riverside Arena", context:"Midweek Conference", role:"Access Steward", area:"Main foyer", date:"2026-10-14", time:"08:00–17:00", report:"07:45", required:2, people:assignments(["Samira Cole","ACCEPTED"],["Owen Blake","ALLOCATED"]), flags:[], sourceId:"EV-109 / REQ-108" },
  { id:"S-205", source:"SITE_SHIFT", client:"Civic Facilities", site:"Civic Hall", context:"Civic Hall Service", role:"Security Officer", area:"Reception", date:"2026-10-12", time:"08:00–17:00", report:"07:45", required:1, people:assignments(["Carla Reid","ACCEPTED"]), flags:[], sourceId:"SS-026 / DEM-205" }
];
const scenarios = [
  {title:"Saturday Event vacancy", day:"2026-10-17", duty:"E-101", note:"Find the Stadium role, read 3 required / 1 open, then review candidates in the exact duty panel."},
  {title:"Declined response", day:"2026-10-17", duty:"E-106", note:"A declined response is retained in source history and does not occupy the current open position."},
  {title:"Approved Time Away", day:"2026-10-17", duty:"E-103", note:"The safe approved Time Away conflict is separate from an availability declaration; the allocation remains visible."},
  {title:"Static Site duty", day:"2026-10-17", duty:"S-201", note:"Ongoing Site Service demand appears beside Event work with its own source identity and guarded route."},
  {title:"Event/static clash", day:"2026-10-17", duty:"S-201", note:"Lewis has overlapping Event and static assignments. Inspect the other source; no automatic cancellation occurs."},
  {title:"Pending versus accepted", day:"2026-10-17", duty:"E-102", note:"Named blocks show Accepted and Awaiting response separately; neither says attended or worked."},
  {title:"Stale write and rejection", day:"2026-10-17", duty:"E-101", note:"Open candidate preview, then simulate a rejected guarded action. The current counts remain unchanged."},
  {title:"390px duty journey", day:"2026-10-17", duty:"E-101", note:"At a genuine 390px viewport, use week strip → selected day → grouped duty agenda → full-height duty panel."},
  {title:"High-volume Saturday", day:"2026-10-17", duty:null, note:"Three Clients, five Sites and ten Saturday duty lines. All seven day columns remain visible at desktop width."},
  {title:"Overnight security", day:"2026-10-16", duty:"S-204", note:"Friday 18:00–Saturday 06:00 appears once on Friday; the next-day finish stays explicit."}
];
const state = {weekOffset:0,day:"2026-10-17",view:"week",filters:{client:"",site:"",context:"",role:"",source:"BOTH",attention:""},panelId:null,candidates:false,simulation:"",scenario:null,personQuery:"",person:"",filtersOpen:false};
const $=s=>document.querySelector(s);
const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]));
const dateOf=(offset,index)=>{const d=new Date(`${BASE}T12:00:00Z`);d.setUTCDate(d.getUTCDate()+offset*7+index);return d.toISOString().slice(0,10)};
const dateIndex=date=>Math.round((new Date(`${date}T12:00:00Z`)-new Date(`${dateOf(state.weekOffset,0)}T12:00:00Z`))/86400000);
const niceDate=date=>new Date(`${date}T12:00:00Z`).toLocaleDateString("en-GB",{timeZone:"Europe/London",day:"numeric",month:"long",year:"numeric"});
const allocated=d=>d.people.length;
const accepted=d=>d.people.filter(p=>p.status==="ACCEPTED").length;
const awaiting=d=>d.people.filter(p=>p.status==="ALLOCATED").length;
const open=d=>Math.max(0,d.required-allocated(d));
const sourceName=d=>d.source==="EVENT"?"Event":"Site Shift";
function attentionMatches(d,value){return !value||value==="open"&&open(d)>0||value==="awaiting"&&awaiting(d)>0||value==="availability"&&d.flags.includes("availability")||value==="leave"&&d.flags.includes("leave")||value==="clash"&&d.flags.includes("clash")}
function currentDuties(){return duties.filter(d=>dateIndex(d.date)>=0&&dateIndex(d.date)<7&&(!state.filters.client||d.client===state.filters.client)&&(!state.filters.site||d.site===state.filters.site)&&(!state.filters.context||d.context===state.filters.context)&&(!state.filters.role||`${d.role} · ${d.area}`===state.filters.role)&&(state.filters.source==="BOTH"||d.source===state.filters.source)&&attentionMatches(d,state.filters.attention))}
function countValues(rows){return {required:rows.reduce((n,d)=>n+d.required,0),allocated:rows.reduce((n,d)=>n+allocated(d),0),awaiting:rows.reduce((n,d)=>n+awaiting(d),0),accepted:rows.reduce((n,d)=>n+accepted(d),0),open:rows.reduce((n,d)=>n+open(d),0)}}
function activeFilterCount(){return Object.entries(state.filters).filter(([k,v])=>k==="source"?v!=="BOTH":Boolean(v)).length}
function optionList(values,selected,all){return `<option value="">${esc(all)}</option>`+[...new Set(values)].sort().map(x=>`<option value="${esc(x)}" ${selected===x?"selected":""}>${esc(x)}</option>`).join("")}
function renderSummary(rows){const c=countValues(rows);const labels={required:"Required",allocated:"Allocated",awaiting:"Awaiting",accepted:"Accepted",open:"Open"};$("#summary").innerHTML=Object.entries(labels).map(([key,label])=>`<button class="summary-item" type="button" data-summary="${key}" data-attention="${key}" aria-pressed="${(key==="open"||key==="awaiting")&&state.filters.attention===key}"><small>${label}</small><strong>${c[key]}</strong><span>${key==="open"||key==="awaiting"?"select to filter":"factual count"}</span>${key==="open"||key==="awaiting"?'<b class="summary-caption">↗</b>':""}</button>`).join("")}
function renderFilters(){const f=state.filters;$("#filters-panel").hidden=!state.filtersOpen;$("#filters-toggle").setAttribute("aria-expanded",String(state.filtersOpen));$("#filter-count").textContent=activeFilterCount()||"";
 $("#filters-panel").innerHTML=`<label>Client<select data-filter="client">${optionList(duties.map(d=>d.client),f.client,"All Clients")}</select></label><label>Site<select data-filter="site">${optionList(duties.map(d=>d.site),f.site,"All Sites")}</select></label><label>Event / Site Service<select data-filter="context">${optionList(duties.map(d=>d.context),f.context,"All sources")}</select></label><label>Role / area<select data-filter="role">${optionList(duties.map(d=>`${d.role} · ${d.area}`),f.role,"All duties")}</select></label><label>Source<select data-filter="source"><option value="BOTH" ${f.source==="BOTH"?"selected":""}>Event + Site Shift</option><option value="EVENT" ${f.source==="EVENT"?"selected":""}>Event only</option><option value="SITE_SHIFT" ${f.source==="SITE_SHIFT"?"selected":""}>Site Shift only</option></select></label><label>Attention<select data-filter="attention"><option value="">All states</option><option value="open" ${f.attention==="open"?"selected":""}>Open positions</option><option value="awaiting" ${f.attention==="awaiting"?"selected":""}>Awaiting response</option><option value="availability" ${f.attention==="availability"?"selected":""}>Availability issue</option><option value="leave" ${f.attention==="leave"?"selected":""}>Approved Time Away conflict</option><option value="clash" ${f.attention==="clash"?"selected":""}>Event/static clash</option></select></label><div class="filter-actions"><button type="button" data-action="clear-filters">Clear filters</button><button type="button" data-action="close-filters">Show duties</button></div>`;
 const note=activeFilterCount()?`<strong>${activeFilterCount()} active filter${activeFilterCount()>1?"s":""}</strong> · Counts and duties shown for this synthetic filtered view.`:"Counts and duties shown for the full synthetic scenario week.";$("#active-filter-note").innerHTML=note;
}
function contextGroup(rows){const result=[];let lastClient="",lastSite="",lastContext="";for(const d of rows){if(d.client!==lastClient){result.push({kind:"client",label:d.client});lastClient=d.client;lastSite="";lastContext=""}if(d.site!==lastSite){result.push({kind:"site",label:d.site});lastSite=d.site;lastContext=""}if(d.context!==lastContext){result.push({kind:"context",label:d.context,source:sourceName(d)});lastContext=d.context}result.push({kind:"duty",d})}return result}
function sorted(rows){return [...rows].sort((a,b)=>a.client.localeCompare(b.client)||a.site.localeCompare(b.site)||a.context.localeCompare(b.context)||a.role.localeCompare(b.role))}
function cell(d){const names=d.people.slice(0,2).map(p=>`<span class="cell-person ${p.status==="ALLOCATED"?"awaiting":"accepted"}">${esc(p.name.split(" ")[0])} · ${p.status==="ALLOCATED"?"Awaiting":"Accepted"}</span>`).join("");return `<button type="button" class="matrix-cell has-duty ${d.date.endsWith("17")||d.date.endsWith("18")?"weekend":""} ${state.panelId===d.id?"selected":""}" data-duty="${d.id}" aria-label="${esc(`${d.client}, ${d.site}, ${d.context}, ${d.role}, ${d.time}, ${d.required} required, ${open(d)} open`)}"><span class="cell-time">${esc(d.time)}</span><span class="cell-coverage">${d.required} required · <em>${open(d)} open</em></span>${names}${d.people.length>2?`<span class="cell-more">+${d.people.length-2} assignments</span>`:""}${d.flags.length?`<span class="cell-flag">${d.flags.length} issue${d.flags.length>1?"s":""}</span>`:""}</button>`}
function scenarioCallout(){const s=scenarios[state.scenario];return s?`<div class="scenario-callout"><strong>SCENARIO ${state.scenario+1} · ${esc(s.title)}</strong>${esc(s.note)}</div>`:""}
function renderWeek(rows){const groups=contextGroup(sorted(rows));const heads=`<div class="matrix-head"><span>CLIENT / SITE / DUTY</span></div>`+days.map((day,i)=>`<div class="matrix-head ${i>=5?"weekend":""} ${state.day===dateOf(state.weekOffset,i)?"selected":""}"><span>${day.toUpperCase()}</span><strong>${dateOf(state.weekOffset,i).slice(-2)}</strong></div>`).join("");const body=groups.map(g=>g.kind!=="duty"?`<div class="hierarchy-row ${g.kind}">${g.kind==="context"?`<span>${esc(g.source)}</span>`:""}${esc(g.label)}</div>`:`<div class="row-label"><strong>${esc(g.d.role)}</strong><span>${esc(g.d.area)}</span><span class="source-tag">${esc(sourceName(g.d))} · ${esc(g.d.id)}</span></div>`+days.map((day,i)=>g.d.date===dateOf(state.weekOffset,i)?cell(g.d):`<div class="matrix-cell empty ${i>=5?"weekend":""}" aria-hidden="true"></div>`).join("")).join("");return `<div class="planner-bar"><strong>Week coverage matrix</strong><span>${rows.length} duty lines · 7 days visible · select a duty for source detail</span></div>${scenarioCallout()}${rows.length?`<div class="matrix-scroll"><div class="matrix">${heads}${body}</div></div>`:`<div class="empty-week"><strong>No duties in this view</strong>This prototype has data for 12–18 October 2026. Clear filters or return to the scenario week.</div>`}`}
function dayStrip(rows){return `<div class="day-strip" aria-label="Select planning day">${days.map((label,i)=>{const date=dateOf(state.weekOffset,i),gap=rows.filter(d=>d.date===date).reduce((n,d)=>n+open(d),0);return `<button type="button" data-day="${date}" aria-pressed="${state.day===date}"><span>${label}</span><strong>${date.slice(-2)}</strong><small>${gap} open</small></button>`}).join("")}</div>`}
function dayCard(d){return `<button class="duty-card" type="button" data-duty="${d.id}" aria-label="Open ${esc(d.context)}, ${esc(d.role)}, ${open(d)} open positions"><div><span class="source-kicker">${esc(sourceName(d))} · ${esc(d.context)}</span><strong>${esc(d.role)} · ${esc(d.area)}</strong><span class="duty-meta">${esc(d.time)} · Report ${esc(d.report)}${d.overnight?" · Finishes next day":""}</span></div><div class="duty-numbers">${d.required} required · <em>${open(d)} open</em></div><div class="duty-status">${accepted(d)} accepted · ${awaiting(d)} awaiting${d.flags.length?` · ${d.flags.length} issue${d.flags.length>1?"s":""}`:""}</div></button>`}
function renderDay(rows){const selected=sorted(rows.filter(d=>d.date===state.day));const byContext=new Map();for(const d of selected){const key=`${d.client}|${d.site}|${d.context}`;if(!byContext.has(key))byContext.set(key,[]);byContext.get(key).push(d)}return `<div class="planner-bar"><strong>Day planner · ${esc(niceDate(state.day))}</strong><span>${selected.length} duty lines · source details on selection</span></div>${scenarioCallout()}${dayStrip(rows)}<div class="day-content">${selected.length?[...byContext.values()].map(group=>`<section class="day-group"><div class="day-group-head"><small>${esc(group[0].client)} · ${esc(group[0].site)} · ${esc(sourceName(group[0]))}</small><strong>${esc(group[0].context)}</strong></div>${group.map(dayCard).join("")}</section>`).join(""):`<p class="day-empty">No demand on this selected day in the filtered synthetic view.</p>`}</div>`}
function renderPerson(){const people=[...new Set(duties.flatMap(d=>d.people.map(p=>p.name)))].sort();const matches=people.filter(p=>p.toLowerCase().includes(state.personQuery.toLowerCase()));const selected=state.person?duties.filter(d=>d.people.some(p=>p.name===state.person)):[];return `<div class="planner-bar"><strong>Authorised Staff context · synthetic preview</strong><span>Operational assignment fields only</span></div><div class="person-workspace"><h2>${state.person?esc(state.person):"Find a Staff schedule"}</h2><p>This prototype searches only its named synthetic assignments. The real workspace must use the existing guarded Staff schedule context, never a broad People search.</p><div class="person-search"><input id="person-search" type="search" placeholder="Search a synthetic Staff name" value="${esc(state.personQuery)}" aria-label="Search synthetic Staff name"><button type="button" data-action="person-search">Search</button></div>${state.person?`<button class="panel-link" type="button" data-action="clear-person">← Choose another Staff member</button>${selected.map(d=>`<div class="person-result"><strong>${esc(niceDate(d.date))} · ${esc(d.time)}</strong><small>${esc(sourceName(d))} · ${esc(d.site)} · ${esc(d.context)} · ${esc(d.role)} · ${esc(d.people.find(p=>p.name===state.person)?.status==="ALLOCATED"?"Awaiting response":"Accepted")}</small><button type="button" data-duty="${d.id}">Inspect exact duty</button></div>`).join("")}`:matches.map(p=>`<div class="person-result"><button type="button" data-person="${esc(p)}">${esc(p)}</button><small>${duties.filter(d=>d.people.some(x=>x.name===p)).length} synthetic assignments in the scenario week</small></div>`).join("")||"<p>No matching synthetic Staff names.</p>"}</div>`}
function panelWarnings(d){return [d.flags.includes("availability")?'<p class="warning">Availability issue · an explicit Unavailable declaration overlaps this allocation. This does not cancel it.</p>':"",d.flags.includes("leave")?'<p class="warning leave">Approved Time Away conflict · safe scheduling status only. Leave reason and evidence are not displayed.</p>':"",d.flags.includes("clash")?'<p class="warning clash">Event/static clash · overlapping assignments across two source records. Inspect and resolve through the guarded source workflows.</p>':""].join("")}
function sourcePath(d){return d.source==="EVENT"?"/events/:eventId?requirement=:requirementId":"/sites/:siteId/services/:serviceId?demand=:demandId"}
let lastPanelId=null;
function renderPanel(){const pane=$("#duty-panel"),overlay=$("#panel-overlay");const d=duties.find(x=>x.id===state.panelId);pane.hidden=overlay.hidden=!d;if(!d){document.body.style.overflow="";lastPanelId=null;return}document.body.style.overflow="hidden";
 const ass=d.people.length?d.people.map(p=>`<div class="assignment"><div><strong>${esc(p.name)}</strong><small>${p.note?esc(p.note):"No issue shown"}</small></div><span class="status-pill ${p.status==="ALLOCATED"?"awaiting":"accepted"}">${p.status==="ALLOCATED"?"Awaiting response":"Accepted"}</span></div>`).join(""):'<p class="candidate-note">No active assignment. Declined/cancelled responses remain in source history.</p>';
 const simulation=state.simulation==="pending"?'<div class="flow-box pending" role="status"><strong>Pending · simulated source action</strong><p>No success is displayed while the request is in flight.</p></div>':state.simulation==="readback"?'<div class="flow-box pending" role="status"><strong>Simulated server acceptance received</strong><p>Waiting for exact guarded readback and planner refresh. Success remains hidden.</p></div>':state.simulation==="success"?'<div class="flow-box success" role="status"><strong>Prototype confirmed state</strong><p>Simulated server acceptance → exact readback → refreshed view → confirmation. No KSS record changed; the factual counts above remain the original fixture.</p></div>':state.simulation==="error"?'<div class="flow-box error" role="alert"><strong>Simulated server rejection</strong><p>Action rejected. Previous authoritative allocation state and counts remain unchanged. No success message.</p></div>':"";
 pane.innerHTML=`<div class="panel-top"><div class="panel-topline"><span class="panel-kicker">${esc(sourceName(d).toUpperCase())} · SYNTHETIC SOURCE ${esc(d.id)}</span><button class="panel-close" type="button" data-action="close-panel" aria-label="Close duty detail">×</button></div><h2 id="panel-title">${esc(d.role)} · ${esc(d.area)}</h2><p>${esc(d.client)} / ${esc(d.site)} / ${esc(d.context)}</p></div><div class="panel-section"><h3>Exact duty context</h3><dl class="panel-facts"><div><dt>Service date</dt><dd>${esc(niceDate(d.date))}</dd></div><div><dt>Report</dt><dd>${esc(d.report)}</dd></div><div><dt>Shift</dt><dd>${esc(d.time)}${d.overnight?" · next-day finish":""}</dd></div><div><dt>Source</dt><dd>${esc(sourceName(d))}</dd></div></dl><div class="source-id">Synthetic source identity: ${esc(d.sourceId)}<br>Existing guarded route shape: ${esc(sourcePath(d))}</div></div><div class="panel-section"><h3>Coverage · factual counts</h3><div class="coverage-grid"><div><strong>${d.required}</strong><span>Required</span></div><div><strong>${allocated(d)}</strong><span>Allocated</span></div><div><strong>${accepted(d)}</strong><span>Accepted</span></div><div><strong>${open(d)}</strong><span>Open</span></div></div><p class="candidate-note">${awaiting(d)} awaiting Staff response. Accepted is not attendance or worked time.</p></div><div class="panel-section"><h3>Active assignments</h3>${ass}${panelWarnings(d)}${d.history?`<p class="candidate-note">${esc(d.history[0])}. Source history is separate from current coverage.</p>`:""}</div><div class="panel-section"><h3>Next guarded step</h3><div class="panel-actions">${open(d)>0?'<button class="primary-button" type="button" data-action="candidates">Review candidates</button>':''}<button class="secondary-button" type="button" data-action="source-workflow">Open source detail</button></div>${state.candidates?`<div class="candidate-list"><div class="candidate-row"><strong>Elena Ward · synthetic candidate</strong><span>Candidate review required in the existing ${esc(sourceName(d))} workflow.</span></div><div class="candidate-row blocked"><strong>Lewis Price · review conflict</strong><span>Event/static clash · no private reason or ranking shown.</span></div></div><p class="candidate-note">Candidate list is a visual fixture, not a live eligibility result. Selecting someone here cannot allocate.</p>`:""}<p class="candidate-note">Select → context → existing source workflow. There is no direct grid write in this prototype.</p></div><div class="panel-section"><h3>Response feedback pattern</h3><span class="simulation-tag">SIMULATION ONLY · NO SERVER CONNECTION</span><p class="candidate-note">Preview the accepted Staff-pilot feedback contract. Neither control changes the fixture or KSS data.</p><div class="panel-actions"><button class="secondary-button" type="button" data-action="simulate-success" ${state.simulation==="pending"||state.simulation==="readback"?"disabled":""}>Preview accepted flow</button><button class="secondary-button" type="button" data-action="simulate-error" ${state.simulation==="pending"||state.simulation==="readback"?"disabled":""}>Preview rejection</button></div>${simulation}</div>${state.scenario!==null?`<div class="panel-section">${scenarioCallout()}</div>`:""}`;
 if(lastPanelId!==d.id)pane.querySelector(".panel-close")?.focus({preventScroll:true});lastPanelId=d.id;
}
function render(){const rows=currentDuties();const isMobile=matchMedia("(max-width:650px)").matches;const displayView=state.view==="person"?"person":isMobile?"day":state.view;$("#week-label").textContent=`${niceDate(dateOf(state.weekOffset,0)).replace(/ 2026$/,"")} – ${niceDate(dateOf(state.weekOffset,6))}`;$("#week-view").setAttribute("aria-pressed",String(displayView==="week"));$("#day-view").setAttribute("aria-pressed",String(displayView==="day"));$("#person-view").setAttribute("aria-pressed",String(displayView==="person"));renderSummary(rows);renderFilters();$("#planner").innerHTML=displayView==="person"?renderPerson():displayView==="day"?renderDay(rows):renderWeek(rows);renderPanel();}
function selectScenario(index){const s=scenarios[index];state.scenario=index;state.weekOffset=0;state.day=s.day;state.filters={client:"",site:"",context:"",role:"",source:"BOTH",attention:""};state.view=index===7?"day":"week";state.panelId=s.duty;state.candidates=false;state.simulation="";state.filtersOpen=false;$("#scenario-list").hidden=true;$("#scenario-toggle").setAttribute("aria-expanded","false");render();$("#announcer").textContent=`Scenario ${index+1}: ${s.title}`;}
function closePanel(){const closingId=state.panelId;state.panelId=null;state.candidates=false;state.simulation="";render();$("#planner").querySelector(`button[data-duty="${closingId}"]`)?.focus({preventScroll:true})}
let simulationRun=0;
function simulate(kind){const run=++simulationRun;state.simulation="pending";renderPanel();setTimeout(()=>{if(run!==simulationRun||!state.panelId)return;state.simulation=kind==="error"?"error":"readback";renderPanel();if(kind==="success")setTimeout(()=>{if(run!==simulationRun||!state.panelId)return;state.simulation="success";renderPanel()},950)},850)}
$("#scenario-list").innerHTML=scenarios.map((s,i)=>`<button type="button" data-scenario="${i}"><small>${String(i+1).padStart(2,"0")} / 10</small>${esc(s.title)}</button>`).join("");
document.addEventListener("click",event=>{const b=event.target.closest("button");if(!b)return;if(b.id==="scenario-toggle"){const open=$("#scenario-list").hidden;$("#scenario-list").hidden=!open;b.setAttribute("aria-expanded",String(open));return}if(b.dataset.scenario!==undefined){selectScenario(Number(b.dataset.scenario));return}if(b.dataset.summary){if(["open","awaiting"].includes(b.dataset.summary)){state.filters.attention=state.filters.attention===b.dataset.summary?"":b.dataset.summary;render()}return}if(b.dataset.day){state.day=b.dataset.day;render();return}if(b.dataset.duty){state.panelId=b.dataset.duty;state.candidates=false;state.simulation="";renderPanel();return}if(b.dataset.person){state.person=b.dataset.person;render();return}if(b.id==="prev-week"||b.id==="next-week"||b.id==="today-week"){state.weekOffset=b.id==="today-week"?0:state.weekOffset+(b.id==="next-week"?1:-1);state.day=dateOf(state.weekOffset,0);state.panelId=null;render();return}if(["week-view","day-view","person-view"].includes(b.id)){state.view=b.id.replace("-view","");state.panelId=null;render();return}if(b.id==="filters-toggle"){state.filtersOpen=!state.filtersOpen;renderFilters();return}const action=b.dataset.action;if(action==="close-filters"){state.filtersOpen=false;renderFilters()}if(action==="clear-filters"){state.filters={client:"",site:"",context:"",role:"",source:"BOTH",attention:""};render()}if(action==="close-panel")closePanel();if(action==="candidates"){state.candidates=!state.candidates;renderPanel()}if(action==="source-workflow"){state.simulation="";$("#announcer").textContent="Prototype only: the real action opens the exact guarded source workflow.";alert("Prototype only: open the exact Event staffing or Site Service demand source in the production design. No KSS data is connected here.")}if(action==="simulate-success")simulate("success");if(action==="simulate-error")simulate("error");if(action==="person-search"){state.personQuery=$("#person-search")?.value??"";state.person="";render()}if(action==="clear-person"){state.person="";render()}});
document.addEventListener("change",event=>{const filter=event.target?.dataset?.filter;if(filter){state.filters[filter]=event.target.value;state.panelId=null;render()}});
$("#panel-overlay").addEventListener("click",closePanel);
document.addEventListener("keydown",event=>{if(event.key==="Escape"&&state.panelId){closePanel();return}if(event.key==="Tab"&&state.panelId){const focusable=[...$("#duty-panel").querySelectorAll("button:not([disabled]),a[href],input:not([disabled])")];if(focusable.length){const first=focusable[0],last=focusable.at(-1);if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus()}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus()}}}if(event.key==="Enter"&&event.target?.id==="person-search"){state.personQuery=event.target.value;state.person="";render()}});
addEventListener("resize",()=>render());
render();
