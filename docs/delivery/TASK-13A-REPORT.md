# TASK-13A delivery report — Operational Control Room

Date: 24 September 2026. **Status:** Accepted by David in synthetic Dev on 24 September 2026 at implementation commit `975f432`. No staging, production, live KSS data, worked-time/pay, new source business write, or deployment change.

## Scope delivered

- Updated `TASK-13A.md` with David's approval: active Operations/Office Admin/Super Admin under accepted operational authority; accepted 09B static attendance; separate Incident Reviewer grant; safe 08D health; 14-day upcoming and seven-day recent windows; 60-second refresh; summary-only large display with no Staff names; **Recent Operations** heading; no manager action count, inferred no-show, worked-time or score.
- Added `/control-room`, `/api/control-room`, the read-only responsive client and a manager-only navigation capability. The four areas show exact Event and Site Service labels and guarded source links. Staffing Required, Allocated, Accepted and Remaining stay separate from factual attendance, explicit availability conflict and unresolved reviews. Site and source filters, bounded pagination, snapshot timestamp, manual/60-second refresh, stale indication and large-display toggle are present. No write route was added.
- Added `public.control_room_snapshot_13a(text,uuid,integer,integer)` as a bounded `STABLE` read. It checks existing active operational authority, composes exact 06A/06B/06C Event and 08A Site Service/demand/allocation identities, invokes the accepted 09A/09B attendance summaries and 07A explicit conflict helper, projects limited 08D health, and exposes Incident count/IDs only through the 12A active Operations plus Incident Reviewer check. It has no dynamic SQL, Person name, private Incident narrative, finance or 10B dependency. The 08D administrator run ledger and direct authenticated source table access remain unchanged.

## Exact Dev migration and security readback

David explicitly reconfirmed `dnfhkmmnlbiabqypclqg` as **synthetic Development** and `kwpgjbxepxuhwxxydaca` as **protected Staging**, authorised the 13A migration for the former only, and required an exact ID recheck. Supabase's project listing returned both distinct active project IDs immediately before application; the target was compared to the literal approved Dev ID. The approved source file was originally named `20260924220000_control_room_13a.sql`. Supabase MCP assigned the actual Dev migration-history version **`20260924174734`** on successful application. The source-controlled file was renamed to `20260924174734_control_room_13a.sql` to match Dev history. Protected Staging was not targeted.

Readback returned migration `control_room_13a` at `20260924174734`; function volatility `s` (`STABLE`), `SECURITY DEFINER=true`, fixed empty `search_path`, exact arguments `(p_source text,p_site uuid,p_offset integer,p_limit integer)`, `anon EXECUTE=false`, `PUBLIC EXECUTE=false`, `authenticated EXECUTE=true`. An unauthenticated database call raised `Control Room read denied`; an active synthetic Operations claim returned a bounded snapshot. The active Operations snapshot omitted `incidents` when no 12A grant existed. No table/RLS grant was widened.

The first two application attempts were rejected by automatic approval review, which classified the Dev ID as staging despite the project mapping. These were not SQL failures and made no DB change. After David's explicit project-ID confirmation, the exact-ID checked application succeeded. This history is retained here.

## Tests and browser evidence

- `tests/control-room.test.mjs` passed against synthetic Dev. It checked Staff denial, invalid source/oversized page rejection, direct attendance/Incident table denial, active Office/Operations/Super reads, bounded pagination uniqueness, Event Required/Allocated/Accepted/Remaining against `deployment_event_summary`, static counts against `site_service_detail`, 09B checked-in/review facts against `attendance_site_overview_09b`, source/site filters, limited 08D projection with no scheduler write, and the 12A grant/revocation boundary. It scanned the returned payload for private Person/Incident/worked-time fields.
- Serial focused regressions passed **4/4**: 13A, accepted 09B static attendance, 12A Incidents and 08D horizon maintenance. The 09B and 12A tests exercised synthetic factual/authority histories; they do not establish live data behavior.
- Focused ESLint and 13A-scoped TypeScript checks passed. `npm run build` passed with Next.js 16.3.6 Webpack after the shared generated-type duplicates cleared; the route table includes `/control-room` and `/api/control-room`. The earlier build failure from duplicate `.next/dev/types/* 2.ts`/`* 3.ts` in the active shared checkout is preserved as historical evidence, not counted as the final build.
- Authenticated local browser: synthetic Office Admin opened `/control-room` and saw all four areas, exact source rows and no Incident card. At **1280px**, document `scrollWidth=1280`; at **390px**, `scrollWidth=390`, one-column cards and visible filter/Refresh/large-display controls; at **1920px**, `scrollWidth=1920`, large-display styling and no Staff names or card action links. The Event source filter reduced the current 28-record mixed snapshot to one Event row; returning to all sources restored the view. An exact Site Service deep link opened its authorised detail page. These were live DOM/screenshot observations in the in-app browser; no screenshot file was saved. 09B's separate desktop/390px visual follow-up remains outside 13A acceptance.

## Limits and acceptance boundary

The browser used an Office Admin persona; Operations, Super Admin, Staff and reviewer/no-reviewer boundaries were tested through authenticated Supabase RPCs, not separate browser personas. The existing synthetic fixture had one current Site shift and a future Event; it did not exercise a current `LIVE` Event with accepted/check-in facts in the browser. The 09A/09B regressions cover those underlying factual transitions. The Control Room read is one database-statement snapshot per page; a later page is a fresh snapshot and may differ if source records change between requests. The UI displays each snapshot time and refreshes, but it does not pin a multi-page historical view. Attention rows are bounded to 50, with the total count shown. No explicit report/export is provided. The current 08D latest run may say “needs review” when a prior run succeeded, and the exact health facts remain visible without a readiness verdict.

## David's acceptance — 24 September 2026

David accepted TASK-13A as the read-only Operational Control Room in synthetic Dev. The delivered four areas are Live Now, Upcoming, Needs Attention and Recent Operations. Staffing, factual attendance, explicit availability conflicts, separately authorised Incident follow-up and static horizon health remain distinct source facts. The Control Room creates no operational state and infers no no-show, completion, worked/payable time or readiness/risk score. Private Incident narrative and Staff details are excluded from the large display.

Migration `20260924174734_control_room_13a.sql` was applied only to confirmed synthetic Dev project `dnfhkmmnlbiabqypclqg`; protected Staging `kwpgjbxepxuhwxxydaca` was not touched. David accepted the focused integration and 09B/12A/08D regressions, full Webpack build, and authenticated Office browser evidence at 1280px, 390px and 1920px.

Separate Operations and Super Admin browser personas, a browser fixture with a current `LIVE` Event, and the independent 09B desktop/390px visual check remain follow-ups. They do not reopen the accepted 13A architecture. Later 14A Handover, 15A Assets and 18A Mobilisation may feed this surface through narrow guarded read projections; their business logic stays with their source modules.

**Boundary:** Acceptance authorises no staging, production, real KSS data, worked-time/pay integration or deployment.
