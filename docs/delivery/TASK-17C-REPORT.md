# TASK-17C — Leave Scheduling Constraint Policy

**Status: ACCEPTED — SYNTHETIC DEV by David on 25 September 2026.** TASK-17B was accepted at `b8cac06`. No TASK-17D work is authorised or included.

## David's formal acceptance — 25 September 2026

David accepts the two applied 17C migrations and the delivered policy: effective `APPROVED` and `CANCELLATION_REQUESTED` Time Away hard-blocks new overlapping Event/Site Shift allocation and Staff acceptance; final approved cancellation ends future blocking. Exact half-open duty intervals and London DST handling remain in force. Time Away, Availability, Allocation, Attendance and Worked Time remain separate sources. Scheduling exposes `APPROVED_TIME_AWAY_CONFLICT` distinctly from `DECLARED_UNAVAILABLE`, without private leave category, reason, notes or history. Leave approval over an existing `ALLOCATED` or `ACCEPTED` duty does not mutate it and creates one exact durable issue per affected allocation/approval revision. Operational resolution uses existing guarded workflows followed by explicit issue closure; historical evidence persists, Time Away managers gain no allocation authority and no retain-conflict override exists.

Accepted evidence comprises focused 17C **4/4**, controlled approval/allocation/acceptance/final-cancellation and Event/static concurrency with both winning orders and no unexplained committed overlap; 06C, 07A, 07B, 08A, 09A, 09B, 10B boundary and accepted 17B regressions; exact Availability, Attendance, Worked Time and demand no-mutation assertions; production Webpack build, sequential TypeScript, focused ESLint and `git diff --check`; fresh authenticated desktop and genuine 390px Staff/Operations sessions, no overflow, 44px controls, visible keyboard focus, safe wording, separate Availability/Time Away presentation, Staff Accept denial and Decline, manager cancellation and explicit reconciliation closure. The complete repository-wide regression suite was **not run**; David accepts this evidence limitation. This acceptance does not authorise TASK-17D, payroll, paid-leave calculation, sickness/medical workflow, automatic cover or reassignment, scheduling override, Staging, production or real Staff leave data. The TASK-17C synthetic Dev lane is closed.

## Scope and applied migrations

The configured Supabase project and URL resolved to `dnfhkmmnlbiabqypclqg`. No migration or test targeted protected staging `kwpgjbxepxuhwxxydaca`, production or real Staff leave data. The work was made in an isolated checkout based on the accepted 17B implementation. The 17C brief's stale 17B status was corrected before coding.

| Dev migration version | Name | Applied purpose |
| --- | --- | --- |
| `20260924222117` | `leave_scheduling_constraint_17c` | Private effective approved Time Away predicate; Event and Site Shift candidate, allocation and Staff response gates; Person lock coordination with 17B approval/cancellation; immutable reconciliation issue/event ledger; guarded operational list and explicit close RPCs; accepted 17B overlap backfill |
| `20260924222558` | `leave_workforce_projection_17c` | Safe Workforce week/person conflict projections and conflict filter |

Migration history readback confirmed both versions in Dev. The first migration's backfill created five Event and eight Site Shift issues for already approved synthetic 17B overlaps. An intermediate readback found 15 `REVIEW_REQUIRED` and 21 explicitly `CLOSED` synthetic issues; later fixture runs increased those counts. The authenticated database role had zero direct read/write table privileges on the two reconciliation tables, and guarded RPC tests denied direct reads/writes and forged IDs.

## Delivered policy

- `APPROVED` and `CANCELLATION_REQUESTED` leave hard block a new overlapping Event or Site Shift allocation and Staff acceptance of an existing `ALLOCATED` duty. Rejected cancellation remains effective; final approved cancellation ends future blocking. Other request states do not hard block.
- The predicate uses half-open exact intervals. Event uses requirement report to shift end, static uses demand report to shift end, and whole days use successive London midnights. Existing availability, capacity and cross-source allocation guards remain in force. Candidate preview is advisory; writes recheck after the Person lock. `APPROVED_TIME_AWAY_CONFLICT` is the only scheduling reason exposed.
- Leave approval over an existing active allocation commits without mutating that allocation. One durable, typed, revision-bound issue is created per affected allocation and approval revision. The issue stores no leave category, reason or notes. Operational managers use existing guarded duty workflows to resolve the duty, then explicitly close the issue after the current source state is rechecked. Cancellation of leave or duty never deletes or automatically closes history; an active approved overlap cannot be closed as an accepted exception.
- Event/static allocation screens show safe candidate conflict text and exact allocation reconciliation controls. Workforce shows a separate approved Time Away indicator and count. Time Away manager detail retains its authorised existing-allocation conflict view. Scheduling roles receive no general Time Away request read authority.

## Synthetic verification

Focused authenticated Dev tests: `tests/leave-scheduling.test.mjs` **4/4 passed** on the final run. They covered Event/static candidate and transaction blocks, Event/static Staff acceptance, whole and partial days, non-overlap and touching boundaries, overnight duty, spring/autumn London DST, nonblocking states, cancellation requested/rejected/final, unchanged existing `ALLOCATED`/`ACCEPTED` allocations on approval, one exact issue per source, explicit closure, stale revisions, idempotent approval retry, forged issue/source IDs, direct table denial, privacy projection and unchanged Availability revision. Five controlled concurrent cases ran with `Promise.all`: approval versus Event allocation, approval versus static allocation, approval versus Staff acceptance, final cancellation versus allocation, and Event versus static allocation. The final run observed Event allocation win followed by an issue, static approval win followed by allocation denial, approval win followed by acceptance denial, final cancellation permitting allocation, and Event winning the shared Person allocation race. Prior runs observed the opposite winning order for some races. All passed the no unexplained committed overlap invariant.

The final focused run used separate available synthetic dates in May–August 2028 to avoid collisions with earlier test fixtures. Earlier fixed-date reruns had failed from already approved synthetic leave, not from the new gate. Final readback found **zero active** Event and zero active Site Shift allocations among `17C` synthetic fixtures. Immutable synthetic request, decision and issue history remains by design.

Accepted 17B Time Away, Event Attendance 09A, static Attendance 09B, Worked Time 10B, Workforce 07B and the relevant staffing DST regression passed in the initial shared Dev run. The Event staffing regression passed with a temporary fixture expectation updated from exactly eight role choices to at least eight; Dev currently has nine, and no accepted test file was changed at that point. Two existing availability/site-shift regression files did not pass unchanged because their synthetic fixtures collided with accumulated Dev records or fell outside the default first 25 deployment results. Temporary wider-page reruns then hit the same fixture collision. These were interim shared-regression evidence gaps, later closed below. No Attendance, Worked Time or demand mutation from leave approval was observed in the guarded source and integration checks at that point; exact before/after assertions were added in the final focused run below.

The final `npm run build` passed Next.js 16.3.6 compile, TypeScript and page generation. Focused ESLint passed. Both migration files parsed as SQL before application.

## Browser evidence and acceptance boundary

An initial in-app browser had no authenticated synthetic Dev session and redirected to sign-in. A request to inspect the existing Safari session was rejected by automatic approval review because it could expose unrelated private browser content. That rejection was not bypassed. David subsequently directed a fresh isolated synthetic-only session. The close-out below uses two named fresh Playwright browser sessions with direct synthetic Staff A and Operations sign-in; no personal Safari session was used.

### Verification close-out — 25 September 2026

Two unchanged shared regression files were reproduced serially before correction:

- `availability.test.mjs` failed at the first new declaration with **Availability replacement confirmation required** because a random future day already held a current synthetic declaration. An earlier run also omitted the exact allocation from the default first 25 `my_deployments` rows. The fixture now searches for a day with no overlapping declaration, deployment or effective approved leave and requests 50 deployment rows. It does not change the Availability or leave guards. Isolated serial rerun: **passed**.
- `site-shifts.test.mjs` failed because neither synthetic Staff A nor B was free in its narrow generated November 2026 fixture window. Readback showed accumulated `DECLARED_UNAVAILABLE` days and active allocations; candidate results retained the correct hard blocks. The fixture now begins seven days ahead, allowing the accepted 56-day generation horizon to offer a larger collision-free window, and requests 50 deployment rows for the exact identity check. It does not weaken candidate, capacity or overlap checks. Isolated serial rerun: **passed**.
- The 06B staffing catalogue assertion expected exactly eight role choices; synthetic Dev currently has nine from another approved lane. It now requires at least the original eight and still checks exact SIA and STEWARD choices. Both 06B staffing checks passed in the resumed serial run.

Fresh authenticated browser proof used synthetic Event `0b60398b-bee5-47fa-a5f3-7e5cb272ef62`, exact allocation `eeefa114-e369-44df-8940-b09fd0e030a5`, submitted Time Away request `1e70fe06-bcce-43e9-a733-7fe44e726b98`, and static demand `5af37549-63b5-42df-bc09-1f2da94e6798`, all in approved Dev. The Time Away manager detail showed the exact `ALLOCATED` Event duty and said no allocation had changed. Approval succeeded through that UI. The operational Event view then showed `review required` on that exact still allocated duty without category, reason, request ID or history; its candidate list separately displayed `Not declared`, `Overlapping allocation` and `Approved time away overlaps this duty`. Staff A's focused My Deployments page rejected Accept with `APPROVED_TIME_AWAY_CONFLICT`, retained `Awaiting your response`, and showed no private leave detail. The operational manager cancelled the duty through its existing guarded Event flow, then explicitly closed the issue. Readback: issue `CLOSED` revision 2, allocation `CANCELLED`, leave still `APPROVED`. The Site Shift manager list showed Staff A `BLOCKED · NOT_DECLARED · Approved time away overlaps this duty` and disabled Allocate; no override was offered.

At genuine 390px, the Staff and Site Shift manager pages each measured `scrollWidth=390` with `viewport=390`. Initial visible action buttons measured 32px tall, below the requested practical 44px target. A scoped CSS correction set Staff deployment, Event allocation sheet and Site Service buttons to at least 44px on mobile. The reconciliation text was corrected after browser evidence showed it still said the allocation was unchanged after authorised cancellation. The saved [Staff 390px](../../output/playwright/task-17c-staff-390-before-touch-fix.png) and [Site Shift 390px](../../output/playwright/task-17c-site-shift-390-before-touch-fix.png) screenshots are explicitly pre-fix evidence; final post-fix measurements and screenshots are recorded below.

After those local changes, the production Webpack build completed compilation, TypeScript and page generation; focused ESLint and `git diff --check` passed. An initial broader serial regression run was interrupted on receipt of a separate read-only Supabase audit pause. Event Attendance 09A and Availability 07A had passed; all later test items were **cancelled by interruption, not failed**. The relevant regressions were resumed after the pause was lifted, as detailed below. No full-suite pass is claimed.

The separate canonical baseline audit was approved and integrated to main at `25dfb02`; its owner explicitly lifted the general synthetic Dev pause for this approved close-out. No new 17C migration was added during close-out. The implementation remains on its isolated branch. No staging, production, real Staff leave, exception override or TASK-17D work is claimed.

### Final readback and remaining limits

After the fixture corrections, the serial relevant regression run passed 14 of 15 checks. The only failure was the existing 07B Workforce test's random week colliding with accumulated effective leave/allocations. Its fixture now selects a week with clear Staff A/B duty windows. The isolated serial 07B rerun passed. Thus each requested directly affected 06C, 07A, 07B, 08A, 09A, 09B, 10B and accepted 17B regression passed, as did the four focused 17C tests; the entire repository suite was **not** run. The focused 17C test was rerun 4/4 after adding before/after assertions for exact Availability revision, Attendance overview, Worked Time self-read and demand revision on leave approval. The final concurrency run observed both allocation-first and approval-first outcomes across its cases, with the required issue-or-denial invariant.

Fresh isolated browser sessions completed the Staff and Operations flows at desktop and genuine 390px. Staff A's focused Event allocation showed `Awaiting your response`; Accept returned the safe `APPROVED_TIME_AWAY_CONFLICT` without changing it. On a second exact synthetic fixture, Staff chose the existing Decline flow while leave remained approved; the browser showed the decline succeeded. The manager view showed that exact allocation as `Declined`, its reconciliation issue remained until explicit manager closure, and readback then showed `CLOSED` issue revision 2, `DECLINED` allocation and still `APPROVED` leave. The earlier manager cancellation fixture similarly read back `CLOSED` issue, `CANCELLED` allocation and still `APPROVED` leave. Readback found zero active `17C` Event and zero active `17C` Site Shift synthetic allocations after guarded cleanup. Immutable history remains.

Post-fix genuine 390px Staff and manager pages both measured `scrollWidth=390` at `viewport=390`. Staff Accept/Decline controls and Site Service week controls measured 44px tall; the Event reconciliation close control measured 44px. Keyboard Tab placed focus on the Staff Decline button with `:focus-visible=true` and a solid outline. Browser snapshots showed non-colour state text, separate `Not declared` Availability and approved-Time-Away labels, disabled allocation for the blocked candidate, wrapping candidate text, and blue/graphite/neutral styling. The reviewed issue text correctly instructs the manager to inspect current allocation state after Staff decline. Screenshots: [Staff 390px](../../output/playwright/task-17c-staff-390-postfix.png), [manager 390px](../../output/playwright/task-17c-manager-390-postfix.png), [Staff desktop](../../output/playwright/task-17c-staff-desktop-postfix.png), [manager desktop](../../output/playwright/task-17c-manager-desktop-postfix.png). These final screenshots show the reviewed/declined state; the earlier pre-fix browser snapshots and screenshots record the blocked acceptance/candidate state. The observed app screen contains only synthetic Dev data.

No browser flow was used to verify every declined/cancelled Time Away state; focused authenticated RPC tests cover those state transitions. The full repository regression suite was not run; David accepted that limitation on 25 September 2026.

Final local checks after the fixture and UI corrections: focused 17C **4/4 passed** with exact no-mutation snapshots; production Webpack build passed; standalone TypeScript passed when run after build generated `.next/types`; focused ESLint passed; `git diff --check` passed. A TypeScript command launched concurrently with the build briefly saw missing generated `.next/types` files while build was replacing them; the sequential rerun returned exit 0. The 07B Workforce fixture correction passed its isolated rerun. No extra Dev migration was applied.
