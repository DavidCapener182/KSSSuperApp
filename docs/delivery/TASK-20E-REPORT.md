# TASK-20E — Training Completion & Certificates: implementation record

**Status: PARTIAL / UNACCEPTED; ready for David's whole-task acceptance review.** Completion Foundation and the bounded Certificate/PDF chain are implemented and verified in synthetic Development. TASK-19A was accepted and closed in synthetic Development on 25 September 2026; its server-authorised private-file boundary is present at the `493df65` cherry-pick. No staging, production, real learner data or deployment.

## Approval and target

David approved the bounded 20E direction on 25 September 2026. Two apply attempts were rejected by automatic approval review because approval relayed from the lead task was not a trusted user message in this task. David then directly approved the exact frozen source, SHA-256 and target in this task. The next apply succeeded. The source bytes have not changed since the approval.

| Item | Exact value |
|---|---|
| Canonical baseline | `25dfb02dd5e1da087b0cc8034b2260d8c129122c` |
| Isolated branch/worktree | `task-20e` at `/private/tmp/kss-task-20e` |
| Literal Supabase project | `dnfhkmmnlbiabqypclqg` |
| Approved source filename before apply | `supabase/migrations/20260925103000_training_completion_20e.sql` |
| Canonical local migration, renamed to match applied ledger | `supabase/migrations/20260925000018_training_completion_20e.sql` |
| Frozen SQL SHA-256 | `2507b146ff0db4e7b8f58115cce375474065dc811a52a151bd58d3faac767ded` |
| Actual remote migration ledger version | `20260925000018` (`training_completion_20e`) |

The Supabase CLI was unavailable in this worktree. The migration file was created locally using the existing repository naming convention, then renamed after apply to match the actual remote ledger version without changing SQL bytes. The remote project `id` and `ref` were positively read as `dnfhkmmnlbiabqypclqg` immediately before successful apply.

## A. Completion Foundation — implemented and verified

- Separate Office Admin `TRAINING_COMPLETION_MANAGER` grant, Super Admin grant/revoke, and attributable grant events. Role alone does not grant manager actions.
- Immutable published `CompletionRuleVersion` pins exact CourseVersion, all-page requirement, exact AssessmentVersion, pass evidence rule, effective interval, optional validity months, publisher/time and rule hash. Publication requires an active Office Training Publisher grant.
- First evaluation pins one rule to an exact Assignment case. Staff may evaluate only their own active assignment; a named Completion Manager may evaluate. Missing page marks or exact submitted PASSED Attempt returns factual unmet codes without a Completion. Successful evaluation creates one Completion and a snapshot of rule hash, page count and qualifying Attempt IDs. Assignment locking, unique keys and request-key payload hashing address concurrent evaluation and replay.
- Reasoned manager void preserves the Completion row and evidence while recording immutable correction history. No score, page mark or Attempt override exists.
- Direct table access to new records is revoked from `anon`/`authenticated`; all new public-schema tables have RLS enabled. Guarded RPCs use actor identity and fixed empty search paths.
- Staff My Learning has a completion check and history projection. Office has rule publication, manager evaluation and correction controls; Super Admin has grant controls.
- A follow-up Office exact-evidence panel reviews one Assignment before evaluation or void. David reviewed and **did not approve** the first read projection, `20260925010000_training_completion_evidence_read_20e.sql`, SHA-256 `1364bf916351ea9572f971ac932f55ddd248ee67323712736d6e2f1aab96f951`, because Staff could receive administrative actor IDs/reasons and raw event details. **Those bytes were never applied.** David directly approved the revised local file `20260925013000_training_completion_evidence_read_safe_20e.sql`, SHA-256 `23bf56e7d317573d4511ff2b2043db51cfab9f58c15e1f9134f699ae3ec6cab3`, at commit `7a26d1b` for synthetic Development only. Literal project `id` and `ref` were rechecked as `dnfhkmmnlbiabqypclqg` and both disk and committed SQL hashes matched before apply. Application succeeded; the **actual remote ledger is `20260925005845 training_completion_evidence_read_safe_20e`**, mapped to the approved local filename above. The frozen original migration remains unchanged.
- The revised history RPC branches explicitly on Staff self versus named manager: Staff gets factual allowlisted fields; manager-only attribution is allowlisted; raw event details are never returned. It adds no certificate, PDF, Storage or business-state writes.

## B. Certificate and PDF — resumed after TASK-19A acceptance

The 19A acceptance cherry-pick adds the accepted server-only private Storage helper and file route pattern without unrelated features in its diff. The 20E implementation uses a separate private `training-certificates` bucket with no ordinary authenticated Storage policy, while sharing the generic secret-key and SHA-256 download helper with 19A. It adds an immutable published template version, explicit manager reservation/finalisation of an issue with its own UUID and opaque reference, pinned Completion/Person/CourseVersion/rule/template, London issue date and source-derived optional expiry, stored PDF hash, attributable revocation and a new linked issue on reissue. Completion void invalidates the current certificate read and download through a fresh join; it does not alter the verified Completion evidence. No Completion action issues automatically. The Staff projection omits manager actor IDs and reasons. No public verification endpoint, Credential adapter, 20F work or temporary direct download mechanism is added.

| Approved certificate migration | Value |
|---|---|
| Canonical local file | `supabase/migrations/20260925104455_training_certificates_20e.sql` |
| Source SHA-256 | `e6290b5262b4a84d1740e74ed8d35bbc7849281afc26e26514bbc88788cbee02` |
| Literal target project `id`/`ref` | `dnfhkmmnlbiabqypclqg` |
| Actual remote ledger version | `20260925105342` (`training_certificates_20e`) |

The Supabase CLI generated the certificate filename. David directly approved exactly the file and SHA-256 above for synthetic Development project `dnfhkmmnlbiabqypclqg` in this task. Disk hash and project `id`/`ref` were rechecked immediately before successful apply, and the disk hash was rechecked unchanged after the final build. The local filename and actual remote ledger version intentionally differ; this row is the mapping.

## Checks actually completed

- SQL syntax parser: 38 statements parsed from the frozen migration. This is syntax only, not execution or security proof.
- TypeScript `tsc --noEmit`: passed after Next generated route types.
- Next 16.3.6 Webpack production build with existing Development environment variables: passed.
- Existing 20C and 20D regression assertions were updated to check that all page marks or a PASSED Attempt alone create no Completion while direct base-table reads remain denied.
- Focused 20B **1/1**, 20C **1/1**, 20D **1/1** and TASK-11A external shortcut **4/4** passed against synthetic Dev. A first combined rerun collided with temporary browser grants; the temporary author/assigner grants were revoked, then 20C and 20D passed sequentially. The remaining temporary Office Completion Manager grant was also revoked after the current browser proof.
- Focused 20E foundation proof: **2/2 passed**. It exercised explicit rule publication, unmet page/pass outcomes, PASSED Attempt and page marks without automatic Completion, exact old/new AssessmentVersion policy, active/cancelled/superseded Assignment, CourseVersion and required AssessmentVersion retirement, historical Completion preservation, rule pinning, concurrent one-ID completion, request replay and changed-payload rejection, peer/Operations/Super role-only denial, guessed IDs, direct table read/write denial, reasoned manager void, named grant and immediate revocation. The separate full evidence projection proof is recorded below.
- Expiry basis readback on the synthetic voided Completion `e5ab5e03-c5eb-4425-ba41-f5753a707d44`: immutable rule `8e6868db-19c7-4c79-bb62-fe739fa4eb6d` records 12 validity months, pinned rule hash matches, London completion date is 25 September 2026 and its calculated factual date is 25 September 2027. A separate null-validity rule returned a null factual date. These are source facts for possible later certificate policy, not credential invalidity, deployment eligibility or an issued certificate.
- Direct exact-ID void readback retained the original Completion, its 1/1 page-mark snapshot and the same current mark, its qualifying submitted PASSED Attempt ID/result, and separate `COMPLETED` and `VOIDED` events with distinct Staff/Office actors and the manager reason. No manual failed-rule Completion was created.
- Scoped cross-domain non-mutation: for synthetic Staff A, exact person-filtered row counts and JSON digests matched before and after one successful Completion evaluation and reasoned void across 19 onboarding, Credentials, Event/Site allocation, Availability, Time Away, Attendance and Event Worked Time tables. No pay/payroll/finance table exists in this Development schema. The synthetic Completion was retained as VOIDED. This readback proves those scoped rows were unchanged; it does not constitute an authenticated candidate-policy journey.
- Authenticated Staff desktop and genuine 390px browser proof showed exact Assignment/CourseVersion, 0/2 then 2/2 page marks, unmet page and assessment messages, PASSED Attempt without automatic Completion, explicit successful check, Completion history and no certificate card. At 390px, `scrollWidth=390`, control height 44px and visible keyboard focus. Screenshots: `output/playwright/task-20e-staff-desktop.png`, `task-20e-staff-check-390.png`, `task-20e-staff-history-390.png`.
- Authenticated Office desktop and genuine 390px browser proof showed the named manager grant, rule publication selector, manager evaluation and reasoned void retaining the original Completion ID. At 390px, `scrollWidth=390`, control height at least 44px, visible keyboard focus, text status cues and blue/neutral styling. Screenshots: `output/playwright/task-20e-office-desktop.png`, `task-20e-office-390.png`. A later exact-evidence panel walkthrough is recorded below.
- Relevant 03A onboarding test passed **1/1**. The 16B state/date boundary test passed **1/1**. The broader 16B Credentials integration test failed in isolation at an existing `latest_revision_id` expectation (`tests/credentials.test.mjs:78`): the current 16B migration sets this value on save. No 16B code or policy was changed here; the integration failure remains a separate regression limitation.
- The approved SQL parsed locally. Remote readback found both `training_completion_history(uuid)` and `training_completion_grants_read()` as `SECURITY DEFINER` with fixed empty `search_path`; EXECUTE belongs to `authenticated` and service roles, not `anon`. The remote definitions match the reviewed Staff/manager allowlist and contain no raw `training_completion_events.details` projection. Supabase security advisors returned existing RLS-without-policy informational findings across guarded tables; the follow-up migration created no table or RLS policy.
- Post-apply focused 20E proof **3/3 passed**, including Staff self-only allowlist, no `evaluatedBy`, `voidedBy`, `voidReason`, event `actorPersonId`, event `reason`, or `details`; exact manager-only attribution without `details`; peer/Operations/Super/ungranted Office denial; and immediate loss of manager access after grant revocation. TypeScript, focused ESLint, `git diff --check` and Next 16.3.6 production Webpack build passed.
- The remote definitions of `private.current_person_id()`, `private.has_active_role(text)` and `private.training_completion_manager()` were read back after David's review. They require an active Supabase Auth identity mapping, a current effective unrevoked `OFFICE_ADMIN` role, and a named Completion Manager grant with `revoked_at is null`. The focused 20E test proved granted Office access followed by immediate denial after revocation; the revised audience-safe history RPC is applied and separately passed the 3/3 read proof.
- Remote ledger readback: `20260925000018 training_completion_20e`. All seven new tables show `rowsecurity=true`; no `anon` or `authenticated` base-table grants were returned. Ten `training_completion_*` public RPCs show `SECURITY DEFINER`, empty `search_path`, and execute ACL only for `authenticated` plus service roles.
- Authenticated Office desktop and genuine 390px follow-up verified the new exact-evidence panel: pinned CourseVersion/content hash, 1/1 and 2/2 page marks, required AssessmentVersion, submitted PASSED Attempt, original Completion ID and page/Attempt snapshot, attributable void actor/reason and event chronology. An active synthetic Completion was voided through the panel with a reason; the same ID and evidence stayed visible with a new `VOIDED` event. At 390px, `scrollWidth=390`, minimum button height 44px and keyboard focus outline 3px solid; status is stated in text. Screenshots: `output/playwright/task-20e-office-evidence-desktop.png`, `task-20e-office-evidence-390.png`, and `task-20e-office-revoked-390.png`. The temporary named Office manager grant was revoked and the signed-in Office browser immediately lost its Assignment review panel after reload; rule publication remained available under its separate grant.
- Certificate SQL: 29 top-level statements parsed before apply. The final approved disk SHA-256 remained `e6290b5262b4a84d1740e74ed8d35bbc7849281afc26e26514bbc88788cbee02`. Post-apply readback: four `training_certificate_*` tables have RLS enabled and no `anon`/`authenticated` base-table grants. All eight public certificate RPCs are `SECURITY DEFINER` with fixed empty `search_path`, EXECUTE for `authenticated` and service roles only. Both private helper functions are inaccessible to ordinary roles. The `training-certificates` bucket is private, capped at 1 MiB and PDF only. No `storage.objects` policy mentions that bucket. Supabase advisors report the expected [RLS-without-policy notice](https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy) for RPC-only tables and existing broader project notices.
- Certificate end-to-end synthetic Development test **1/1 passed**. It proved no issue from page marks, PASSED Attempt or Completion alone; explicit manager issue only; one winner under concurrent issue; same request replay returning the same issue and changed replay denied; Staff own current history and PDF; peer, Operations, ungranted Office and direct Storage denial; private PDF SHA-256 against the recorded hash; named manager access and immediate loss after grant revocation; reasoned revoke and fresh-session download denial; linked reissue with distinct issue, private object and hash while the revoked original PDF/hash remain unchanged; and Completion void invalidating current download while retaining Completion and passed evidence.
- Around a second passing certificate issue/revoke/reissue/Completion-void run, exact Staff A row counts and sorted JSON digests stayed equal across 19 person-filtered non-Training tables: Credentials, onboarding, Event/Site allocation, availability, time away, attendance and event worked time. Eighteen of those tables held rows; all 19 had no change. This is a scoped synthetic non-mutation readback, not a full application-wide audit.
- The accepted TASK-19A private-file regression and affected Training 20B, 20C, 20D and 20E source lifecycle tests all passed sequentially (**6/6**). The first combined run also included the 20E evidence projection test, which failed its expected ungranted-Office assertion while the temporary Office browser manager grant was active. After that grant was revoked and read back, the same 20E evidence test passed **1/1**, preserving the Staff/manager privacy distinction. The temporary browser manager grant is no longer active.
- The requested TASK-11A external Training shortcut regression passed **4/4** after the certificate implementation.
- Authenticated Office desktop and genuine 390px browser walkthrough selected exact Assignment `9664c809-d21a-44d6-b3c1-01742428780a`, issued one certificate from published template v1 with reason, and read back issue reference `KSS-T-7FF6AB2D857E4F4D86A2`, `RESERVED`/`ISSUED` events and the private PDF link. The panel fit 1280px and 390px (`scrollWidth` equal to viewport). Evidence: `output/playwright/task-20e-certificate-office-desktop.png` and `task-20e-certificate-office-390px.png`. After manager grant revocation, the same Office browser lost Completion review controls on reload; its separate publisher controls remained.
- Authenticated Staff desktop and genuine 390px walkthrough showed the same current issue/reference and app-authorised private PDF link in the learner's own Completion history, alongside the clear distinction that Completion itself is not a certificate. Both viewports had no horizontal overflow; the 390px card was visually inspected after spacing changes. Evidence: `output/playwright/task-20e-certificate-staff-current-desktop.png` and `task-20e-certificate-staff-current-390px.png`.
- A synthetic Unicode-name sample PDF rendered and its PNG was visually inspected. A narrow Supabase SSR cookie-write guard for Server Components and Proxy response header forwarding were added after a real Staff refresh hit a cookie mutation error; the authenticated page then loaded. Final TypeScript, focused ESLint, `git diff --check` and Next 16.3.6 Webpack production build passed.
- Full repository regression was not run.

## Acceptance boundary

The bounded Completion Foundation → template → explicit manager issue → immutable certificate/reference → private PDF/hash → freshly authorised server download → revoke/reissue → Completion-void invalidation chain is ready for one whole-task acceptance decision. This report does not infer human acceptance, staging/production readiness, or authorisation for 20F, Credentials integration, automatic issue or public download.
